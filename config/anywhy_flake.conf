# Shared logging config for all builds
# Disable minimal logging to allow ZMK debug messages (set CONFIG_ZMK_LOGGING_MINIMAL=y to suppress)
# CONFIG_ZMK_LOGGING_MINIMAL is not set
CONFIG_LOG_MODE_IMMEDIATE=y
