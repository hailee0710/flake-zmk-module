CONFIG_ZMK_POINTING=y

# Required for PS/2 UART driver to receive trackpoint data via interrupts.
# Without this, the UART won't use interrupt-driven mode and PS/2 reads fail.
CONFIG_UART_INTERRUPT_DRIVEN=y
